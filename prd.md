# 空间积木拼合教学工具 — 产品设计文档

## 一、核心设计原则
1. **单屏零滚动**：页面采用左右刚性分栏，所有操作与展示在一屏内完成，禁止整页纵向滚动。
2. **输入即所见**：左侧定义堆积体，右侧3D场景实时同步，降低认知延迟。
3. **教学可视化**：不仅给出缺失图形，必须同步展示"数量法"和"分层减法"的推导过程，让用户明白答案的来源。

---

## 二、页面布局（左右分栏 35% : 65%）

### 左侧面板：构造定义区（固定宽度，内部局部滚动）
从上到下分为三个刚性模块：

#### 模块A：完整堆积体定义（高度占左侧面板40%）
- **尺寸设定栏**：横向排列三个数字输入框，设定长(L)×宽(W)×高(H)，范围3-6，默认4×2×3。
- **层网格编辑器**：显示当前选中层的L×W二维网格（如4×2即8个格子）。点击格子切换"有/无"方块，**有方块时填充深灰色（#495057）并显示白色数字"1"**。
- **层切换标签**：横向排列H个圆角按钮（1, 2, 3...），当前选中层高亮（主色填充），已编辑层显示小绿点标记。
- **快捷操作**："清空本层"、"复制上一层"（方便快速搭建）。

#### 模块B：拆分堆积体管理（高度占左侧面板35%）
- **拆分体卡片列表**：纵向紧凑排列，每个卡片包含：
  - 左侧色块（系统预设鲜艳色，如红#FF6B6B、青#4ECDC4、蓝#45B7D1）
  - 名称（自动命名"拆分体①"、"拆分体②"...）
  - 小方块数量（实时统计）
  - 删除按钮（×）
- **"添加拆分体"按钮**：点击后展开与模块A同构的层编辑器，但额外带一个**颜色选择器**（圆形色块单选）。定义完成后点击"确认加入"。

#### 模块C：主操作区（高度占左侧面板25%，底部固定）
- **主按钮**："计算缺失体"（全宽，高对比色，如深蓝#1D3557，白字）。
- **次按钮行**："重置所有"（灰色幽灵按钮）、"加载真题示例"（下拉选择2019国考/2018青海等经典题型）。

---

### 右侧区域：3D可视化与解析区（自适应剩余宽度）

#### 区域A：3D主视口（占右侧高度75%）
- **WebGL画布**：全宽全高，背景色与页面统一（#F8F9FA）。
- **场景元素**：
  - **完整堆积体**：以**10%透明度浅灰实体 + 白色线框**显示，作为"目标容器"参考，不抢夺视觉焦点。
  - **已知拆分体**：以**不饱和鲜艳色实体**显示（使用模块B中指定的颜色），带柔和投影，增强立体感。
  - **缺失堆积体（计算后）**：以**高饱和明黄色（#FFD93D）实体**显示，边缘加粗（#FF9F1C），并带轻微发光效果，确保一眼识别。
  - **地面辅助**：淡色网格（#DEE2E6）和坐标轴指示器（左下角小立方体）。
- **相机控制**：默认等轴测视角（斜45°俯视），支持鼠标左键旋转、右键平移、滚轮缩放，带阻尼感。

#### 区域B：求解结果与解析面板（占右侧高度25%，底部固定，浅色底#FFFFFF带顶边框）
该面板是**教学核心**，分为三栏横向排列：

- **栏1：数量法推导（宽度30%）**
  - 标题："数量计算"
  - 内容形式：等宽字体算式，动态填充。
  - 示例：`完整体: 22 = ①(5) + ②(6) + ③(5) + 缺失(6)`
  - 缺失数字在计算后以动画方式（数字滚动增长）填入，并高亮黄色背景。

- **栏2：分层减法视图（宽度40%）**
  - 标题："第N层俯视图"（带左右箭头切换层数）。
  - 显示当前层的L×W网格：
    - 完整体轮廓以浅灰底表示。
    - 已知拆分体占据的格子填充对应颜色（半透明）。
    - **缺失格子以明黄色斜纹或实心填充**，直观显示"这一层缺在哪"。
  - 文字标注："本层总计X格，已占Y格，缺Z格"。

- **栏3：操作与提示（宽度30%）**
  - 按钮："播放拼合动画"（点击后已知拆分体依次飞入完整体，缺失体最后生长填入）。
  - 按钮："切换透明模式"（将完整体变为纯线框，不遮挡内部缺失块）。
  - 简短文字提示：如"观察第2层右下角凹陷，缺失体应有对应凸起"。

---

## 三、核心交互流程

### 1. 定义完整堆积体
用户设定L,W,H → 点击层号（如"第3层"）→ 在网格中点击激活格子 → 右侧3D区实时生成灰色透明线框体。

### 2. 定义拆分堆积体
点击"添加拆分体" → 选择颜色 → 逐层在网格中定义（系统强制限制：只能在完整体已有方块的位置放置，超出则提示"不可超出完整体范围"）→ 点击确认 → 右侧3D区以该颜色渲染新拆分体，并**自动吸附**到与完整体重叠的位置（无需用户手动在3D空间摆放）。

### 3. 计算与揭示（关键教学时刻）
点击"计算缺失体"后：
1. **系统执行三维布尔减法**：完整体体素集合 减去 各拆分体体素集合，得到缺失体素集合。
2. **3D区动画**：缺失体素以黄色立方体形式从中心点放大弹出（scale 0→1，时长0.4s，ease-out-back缓动）。
3. **解析面板激活**：
   - 数量公式自动计算并滚动数字。
   - 分层视图默认展示第1层，缺失格高亮。
   - 若缺失体由多个不连通部分组成，提示"注意：剩余形状包含N个分离部分"。

### 4. 辅助理解操作
- **视角锁定**：3D区右上角提供四个小按钮："正视图"、"俯视图"、"侧视图"、"自由视角"。点击后相机平滑过渡到标准角度，对应考公题目给出的视图。
- **拼合动画**：点击后，各拆分体从分散状态平移到完整体内的对应位置，缺失体最后填入，展示"凹凸一致"的拼合逻辑。

---

## 四、视觉与美学规范（确保简洁美观）

### 色彩系统
| 元素 | 色值 | 说明 |
|------|------|------|
| 页面背景 | #F8F9FA | 极浅灰，护眼不刺眼 |
| 面板背景 | #FFFFFF | 纯白卡片，1px边框#E9ECEF，圆角8px |
| 完整堆积体 | 填充rgba(108,117,125,0.1)，线框#ADB5BD | 极淡，作为背景参考 |
| 拆分体① | #FF6B6B | 珊瑚红 |
| 拆分体② | #4ECDC4 | 青绿 |
| 拆分体③ | #45B7D1 | 天蓝 |
| 缺失堆积体 | #FFD93D（填充），#FF9F1C（边框） | 明黄高警示色 |
| 主按钮 | #1D3557 | 深海蓝，稳重 |
| 文字主色 | #212529 | 近黑 |
| 文字次要 | #6C757D | 灰 |

### 字体与间距
- 全局字体：`system-ui, -apple-system, sans-serif`
- 标题：14px，font-weight: 600，letter-spacing: 0.5px
- 正文/数字：13px，数字使用等宽字体（`ui-monospace, SFMono-Regular`）
- 模块间距：16px，内部padding：12px

### 3D场景美学
- 光源：环境光（强度0.5）+ 平行光（强度0.9，从右上方45°照射，产生左侧柔和阴影）。
- 材质：拆分体使用`MeshStandardMaterial`，粗糙度0.4，金属度0.1，避免塑料感过强。
- 相机：透视相机，FOV 45°，初始位置`(10, 10, 10)`看向原点。

---

## 五、边界与异常处理
1. **拆分体重叠**：若用户在定义两个拆分体时放置了同一坐标方块，点击确认时提示"拆分体之间不能占据同一方块，请调整"。
2. **超量放置**：拆分体某层激活的格子数超过完整体对应层，禁止确认并标红冲突格。
3. **无解/零解**：若拆分体已填满完整体，点击计算后提示"无缺失部分，已构成完整堆积体"；若剩余方块为0，提示相同内容。
4. **性能上限**：限制最大网格为6×6×6（216个方块），超过则禁止生成，确保网页端60fps流畅。

---

## 六、教学功能清单（确保"知道答案怎么出来的"）

| 功能 | 教学目的 |
|------|----------|
| **数量法公式动画** | 直观展示"总数22 - 已知16 = 缺失6"的秒杀技巧 |
| **分层俯视图减法** | 展示每一层"哪里被占了、哪里还空着"，对应"凹凸一致"原则 |
| **透明/线框切换** | 让用户看穿完整体内部，理解被遮挡位置默认有方块 |
| **标准视角切换** | 对应考公题目给出的正视图、后视图，建立平面与立体的映射 |
| **拼合飞入动画** | 动态演示"凹凸对应、旋转翻转后拼合"的空间过程 |

---

## 七、技术实现提示（非代码，仅架构）
- **3D引擎**：Three.js（成熟、轻量、社区庞大）。
- **状态管理**：左侧输入与右侧3D场景共享一个三维数组状态（`space[H][W][L]`，值为0表示空，1表示完整体，2/3/4...表示各拆分体）。
- **缺失计算**：对三维数组逐体素做集合差运算（`complete - piece1 - piece2 - ...`），结果体素标记为缺失。
- **网格输入**：使用HTML+CSS绘制二维网格，通过点击事件修改状态数组，触发3D场景重绘。

---

此设计确保了**零滚动单屏体验**、**输入友好性**（分层网格点击远胜于3D空间直接摆放）、**教学深度**（数量法+分层视图双揭示），且视觉风格克制现代，无冗余元素，可直接交付开发。